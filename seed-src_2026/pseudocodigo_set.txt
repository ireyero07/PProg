Funcion set_add:
Declaro variables de entrada set e id

Compruebo que set no sea nulo, que el id no sea -1 y que el numero 
de ids no supere el maximo. Si alguna falla: 
    devuelve ERROR

Buscamos si los ids se repiten (con la funcion set_find). Si devuelve 
algun valor distinto de -1:
    la funcion de añadir devuelve ERROR

Guardo el nuevo id en la ultima posicion del set, y aumento en numero de ids

Devuelvo OK si todo fue bien


Funcion set_del:
Declaro variables de entrada set e id

Compruebo que set no sea nulo, que el id no sea -1 y que el numero 
de ids no supere el maximo. Si alguna falla: 
    devuelve ERROR

Buscamos si los ids se repiten (con la funcion set_find). 
    devuelve la posicion en la que se repiten

Si esa posicion es -1: 
    devuelve ERROR

Igualamos una variable a la posicion donde los ids se repiten, y la 
va a recorrer hasta que iguale el numero total de ids, sumando de una
en una. En ella, copiara el id que esta el la siguiente posicion en la 
posicion actual del id, para conseguir ajustar el array de set->ids.

Restamos 1 el numero de ids total

Devuelvo OK si todo fue bien
